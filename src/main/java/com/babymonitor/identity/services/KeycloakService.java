package com.babymonitor.identity.services;



public class KeycloakService {

    public void assignRoleToUser(String userId, String roleName) {
        // Hier kun je de gebruiker ophalen en rol toewijzen
        //var realm = keycloak.realm("your-realm");
        //var userResource = realm.users().get(userId);

        //var role = realm.roles().get(roleName).toRepresentation();
        //userResource.roles().realmLevel().add(Collections.singletonList(role));
    }
}
